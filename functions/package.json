{
  "name": "bookswap-functions",
  "private": true,
  "engines": {"node": ">=20"},
  "type": "module",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1",
    "nodemailer": "^6.9.15",
    "uuid": "^9.0.1"
  }
}
